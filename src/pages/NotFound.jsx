

export default function NotFound () {

    return (
        <div className="not-found">
            <h1
            style = {{
            textAlign: "center",
            textTransform: "uppercase",
            fontSize: "40px",
            cursor: "pointer",
            fontWeight: "bold",
            }}
            >Page Not Found</h1>
        </div>
    )
}